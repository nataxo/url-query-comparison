{
  "scripts": {
    "now-build": "next build && next export -o dist",
    "now-dev": "next -p $PORT",
    "test": "jest"
  },
  "dependencies": {
    "next": "latest",
    "react": "latest",
    "react-dom": "latest",
    "url-parse": "^1.4.7"
  },
  "devDependencies": {
    "enzyme": "^3.10.0",
    "enzyme-adapter-react-16": "^1.14.0",
    "jest": "^24.8.0"
  }
}
